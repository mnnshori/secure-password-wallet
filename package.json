{
  "name": "secure-password-wallet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix auth-service\" \"npm run dev --prefix vault-service\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix auth-service && npm install --prefix vault-service && npm install --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
